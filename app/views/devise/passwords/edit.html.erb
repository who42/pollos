<div class="row">
  <div class="col-lg-4 col-lg-offset-4 well">
    <h3 class="text-center">Canvia el PASSWORD</h3>
    <br>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
      <% if resource.errors.any? %>
        <div class="alert alert-danger alert-dismissable">
          <%= resource.errors.full_messages.first %>
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        </div>
      <% end %>
      <%= f.hidden_field :reset_password_token %>
      <div class="form-group">
        <%= f.label :password, "Nou Password" %><br />
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> caracter mínim)</em>
        <% end %><br />
        <%= f.password_field :password, autofocus: true, autocomplete: 'off', class: 'form-control', placeholder: 'Password' %>
      </div>
      <div class="form-group">
        <%= f.label :password_confirmation, 'Confirma el nou password'%><br />
        <%= f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control', placeholder: 'Repeteix el password' %>
      </div>
      <div class="actions">
        <%= f.submit 'Canvia el password', class: 'btn btn-primary' %>
      </div>
    <% end %>
    <br>
    <%= render "devise/shared/links" %>
  </div>
</div>